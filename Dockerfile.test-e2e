FROM btc-test-full

# Copy test script
COPY scripts/test_system.py /app/scripts/

# Install requests if not already available
RUN pip install --no-cache-dir requests

WORKDIR /app

CMD ["python", "scripts/test_system.py"]